spring:
  application:
    name: ms-sos
  datasource:
    url: jdbc:postgresql://localhost:5432/user-database
    username: postgres
    password: Postgres1234567890
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  data:
    redis:
      notify-keyspace-events: Ex
      host: localhost
      port: 6379
  websocket:
    allowed-origins: "*"
  mail:
    host: smtp.gmail.com
    port: 587
    username: aqsinsteam@gmail.com
    password: rgss lcsc nxzv qvgs
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true
          ssl.trust: smtp.gmail.com
  cache:
    type: redis

server:
  port: 8083
app:
  sos:
    timeout-minutes: 3
    inactivity-threshold-minutes: 15
    location-ttl-minutes: 20
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true
